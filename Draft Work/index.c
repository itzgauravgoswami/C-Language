#include <stdio.h>
int main()
{
    int a,b;
    printf("Enter an integer: ");
    scanf("%d" "%d", &a , &b);

    if (a>b)
    {
        printf("%d is greater", a);
    }
    else
    {
        printf("%d is greater", b);
    }
}

break;

#include <stdio.h>
int main()
{
    char a,b;
    scanf("%d %d", &a, &b);
    case(n)
    {
        case 
    }
}